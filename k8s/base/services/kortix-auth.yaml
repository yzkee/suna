apiVersion: apps/v1
kind: Deployment
metadata:
  name: kortix-auth
  labels:
    app: kortix-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kortix-auth
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: kortix-auth
    spec:
      containers:
        - name: kortix-auth
          image: kortix/kortix-auth:latest
          ports:
            - containerPort: 8009
          envFrom:
            - configMapRef:
                name: kortix-auth-config
                optional: true
            - secretRef:
                name: kortix-auth-secrets
                optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8009
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8009
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kortix-auth
  labels:
    app: kortix-auth
spec:
  type: ClusterIP
  selector:
    app: kortix-auth
  ports:
    - port: 8009
      targetPort: 8009
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kortix-auth
  labels:
    app: kortix-auth
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kortix-auth
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
