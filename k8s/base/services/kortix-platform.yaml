apiVersion: apps/v1
kind: Deployment
metadata:
  name: kortix-platform
  labels:
    app: kortix-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kortix-platform
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: kortix-platform
    spec:
      containers:
        - name: kortix-platform
          image: kortix/kortix-platform:latest
          ports:
            - containerPort: 8012
          envFrom:
            - configMapRef:
                name: kortix-platform-config
                optional: true
            - secretRef:
                name: kortix-platform-secrets
                optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8012
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8012
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kortix-platform
  labels:
    app: kortix-platform
spec:
  type: ClusterIP
  selector:
    app: kortix-platform
  ports:
    - port: 8012
      targetPort: 8012
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kortix-platform
  labels:
    app: kortix-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kortix-platform
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
