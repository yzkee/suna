apiVersion: apps/v1
kind: Deployment
metadata:
  name: kortix-router
  labels:
    app: kortix-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kortix-router
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: kortix-router
    spec:
      containers:
        - name: kortix-router
          image: kortix/kortix-router:latest
          ports:
            - containerPort: 8008
          envFrom:
            - configMapRef:
                name: kortix-router-config
                optional: true
            - secretRef:
                name: kortix-router-secrets
                optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kortix-router
  labels:
    app: kortix-router
spec:
  type: ClusterIP
  selector:
    app: kortix-router
  ports:
    - port: 8008
      targetPort: 8008
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kortix-router
  labels:
    app: kortix-router
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kortix-router
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
