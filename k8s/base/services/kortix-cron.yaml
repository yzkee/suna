apiVersion: apps/v1
kind: Deployment
metadata:
  name: kortix-cron
  labels:
    app: kortix-cron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kortix-cron
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: kortix-cron
    spec:
      containers:
        - name: kortix-cron
          image: kortix/kortix-cron:latest
          ports:
            - containerPort: 8011
          envFrom:
            - configMapRef:
                name: kortix-cron-config
                optional: true
            - secretRef:
                name: kortix-cron-secrets
                optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8011
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8011
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kortix-cron
  labels:
    app: kortix-cron
spec:
  type: ClusterIP
  selector:
    app: kortix-cron
  ports:
    - port: 8011
      targetPort: 8011
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kortix-cron
  labels:
    app: kortix-cron
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kortix-cron
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
