#!/usr/bin/with-contenv bash
# OpenCode Web UI — DEV MODE

cd /workspace

mkdir -p /workspace/.local/share/opencode/log \
         /workspace/.local/share/opencode/storage \
         /workspace/.local/share/opencode/snapshot
chown -R abc:abc /workspace/.local/share/opencode

if [ ! -f /opt/opencode-src/packages/opencode/src/server/server.ts ]; then
  echo "[opencode-web] No local source found, using global binary."
  exec s6-setuidgid abc \
    env HOME=/workspace \
        OPENCODE_CONFIG_DIR=/opt/opencode \
        PATH="/opt/bun/bin:/usr/local/bin:/usr/bin:/bin" \
    opencode web --port 3111 --hostname 0.0.0.0
fi

echo "[opencode-web] Starting from LOCAL SOURCE (/opt/opencode-src)"

exec s6-setuidgid abc \
  env HOME=/workspace \
      OPENCODE_CONFIG_DIR=/opt/opencode \
      GIT_DIR= \
      PATH="/opt/bun/bin:/usr/local/bin:/usr/bin:/bin" \
  /opt/bun/bin/bun run --conditions=browser --cwd /opt/opencode-src/packages/opencode /opt/opencode-dev/web-entry.ts
