/**
 * Unified Markdown System
 * 
 * Single source of truth for all markdown rendering.
 * Import from here to ensure consistency across the entire application.
 * 
 * All markdown uses the same consistent spacing and styling - no variants.
 */

export { UnifiedMarkdown } from './unified-markdown';
export type { UnifiedMarkdownProps } from './unified-markdown';
