'use client';

/**
 * Component that ensures error handler is loaded on client
 * The error handler is already initialized via import in layout.tsx
 * This component is kept for backwards compatibility
 */
export function ErrorHandlerInit() {
  return null;
}

