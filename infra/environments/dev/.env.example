# =============================================================================
# Suna Dev Environment Variables
# =============================================================================
# No Pulumi config needed — all values are hardcoded in index.ts.
# These are only needed as shell env vars when running pulumi commands.
# =============================================================================

# AWS credentials (if not using AWS CLI default profile / SSO)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-west-2

# Pulumi backend (if using local state)
# PULUMI_BACKEND_URL=file://.
# PULUMI_CONFIG_PASSPHRASE=
