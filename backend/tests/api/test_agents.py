"""
Tests for agent-related endpoints

Note: Tests that start agent runs require billing/credits.
If the test user doesn't have billing set up, these tests will be skipped.
"""
import pytest
import httpx